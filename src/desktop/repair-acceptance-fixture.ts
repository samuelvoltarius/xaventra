/**
 * Immutable source marker used only by the disposable packaged-Desktop repair
 * acceptance. It gives the signed activation contract a real tracked source
 * input without changing production behaviour.
 */
export const DISPOSABLE_REPAIR_ACCEPTANCE_STATE = 'fault'
